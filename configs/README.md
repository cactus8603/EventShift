# Configs

```text
eventshift/               Clean entry configs used by scripts/train.sh
mask2former/              Final/rebuild Mask2Former configs
mask2former_experiments/  Historical Swin-L event/RGB experiment configs
segformer/                Final/rebuild SegFormer configs
mmseg/                    MMSeg training configs
maskdino/                 MaskDINO configs
```

The `mask2former_experiments/` files were moved one directory deeper from the
original 0411 bundle, and their `_BASE_` references were adjusted accordingly.
