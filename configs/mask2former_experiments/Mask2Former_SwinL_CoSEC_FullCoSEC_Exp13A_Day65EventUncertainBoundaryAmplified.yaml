_BASE_: Mask2Former_SwinL_CoSEC_FullCoSEC_Exp12A_Day65EventUncertainBoundary.yaml
MODEL:
  DAY_EVENT_BOUNDARY_REFINER:
    # Exp12A made the day candidate region much healthier than Exp11A, but the
    # final update was still almost zero. Exp13A keeps the same event + RGB
    # uncertainty/boundary candidate definition and only amplifies trusted final
    # corrections while reducing repair-negative pressure.
    INIT_ALPHA: 0.030
    GATE_BIAS: -3.0
    LOSS_PRESERVE_KL_WEIGHT: 2.0
    LOSS_GATE_SPARSE_WEIGHT: 0.002
    LOSS_GATE_INVALID_WEIGHT: 0.006
    REPAIR_GATE_POSITIVE_WEIGHT: 12.0
    REPAIR_GATE_NEGATIVE_WEIGHT: 0.5
SOLVER:
  IMS_PER_BATCH: 8
  BASE_LR: 0.000004
OUTPUT_DIR: "work_dirs/fullcosec-exp13a_day65_event_uncertain_boundary_amplified_bs8"
